services:
  app:
    image: ghcr.io/rohirik/personal-coffeshop:latest
    container_name: coffee-shop
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
    restart: always
# Command to run:
# docker-compose up -d
